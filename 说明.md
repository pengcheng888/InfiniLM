# DeepSeek-V4 Sparse FlashMLA KV Cache 布局说明

本文记录以下四个量的含义、来源和关系，便于后续修改 Sparse FlashMLA KV cache 时查阅：

```cpp
qk_nope_head_dim       = 448;
qk_rope_head_dim       = 64;
value_bytes_per_token  = 576;
bytes_per_token        = 584;
```

`qk_nope_head_dim` 和 `qk_rope_head_dim` 来自 `ModelConfig`，并作为
`SparseFlashMLAImpl::create_layer_kv_cache` 的入参。当前实现不再为它们额外定义
`kSparseFlashMlaNopeHeadDim` 和 `kSparseFlashMlaRopeHeadDim` 常量。

`value_bytes_per_token` 和 `bytes_per_token` 在创建 cache 时由上述维度计算；
对当前 DeepSeek-V4 配置，其数值分别为 `576` 和 `584`。

当前定义位置：

```text
csrc/layers/mla_attention/flashmla.cpp
```

## 1. 维度来源

DeepSeek-V4 模型配置中：

```text
head_dim = 512
qk_rope_head_dim = 64
```

NoPE 维度由下面的关系得到：

```text
qk_nope_head_dim
    = head_dim - qk_rope_head_dim
    = 512 - 64
    = 448
```

因此：

```text
qk_nope_head_dim = 448
qk_rope_head_dim = 64

448 + 64 = 512 = head_dim
```

`qk_nope_head_dim` 和 `qk_rope_head_dim` 的单位是元素个数，不是字节数。

## 2. 每个 token 的数据区

NoPE 部分使用 FP8 E4M3 存储，每个元素占 1 字节：

```text
NoPE bytes = 448 * sizeof(FP8) = 448 * 1 = 448
```

RoPE 部分为了保持精度，使用 BF16 存储，每个元素占 2 字节：

```text
RoPE bytes = 64 * sizeof(BF16) = 64 * 2 = 128
```

所以每个 token 的有效 KV 数据区大小为：

```text
value_bytes_per_token
    = NoPE bytes + RoPE bytes
    = 448 + 128
    = 576
```

这里的 `ValueBytesPerToken` 表示不包含量化 scale 的 KV 数据字节数，不是 attention 中的 value head dimension。

## 3. 每个 token 的 scale 区

448 个 NoPE 元素按照每 64 个元素一组进行 FP8 量化：

```text
quantization groups = 448 / 64 = 7
```

每组使用一个 UE8M0 指数 scale，每个 scale 占 1 字节，因此实际保存 7 个 scale 字节。

当前 FlashMLA raw-cache 格式为每个 token 预留 8 字节 scale 区：

```text
7 个有效 scale 字节 + 1 个保留/填充字节 = 8 字节
```

InfiniCore 的 `fused_store_flashmla_cache_` 当前只写入其中 7 个有效字节。第 8 个字节保持为初始化值，因此 raw cache 使用 `Tensor::zeros` 创建。

## 4. 每个 token 的总存储预算

每个 token 的总字节数为有效 KV 数据区加 scale 区：

```text
bytes_per_token
    = value_bytes_per_token + scale bytes
    = 576 + 8
    = 584
```

四个量的完整关系为：

```text
head_dim
    = NoPE dimension + RoPE dimension
    = 448 + 64
    = 512

value bytes per token
    = NoPE dimension * sizeof(FP8)
    + RoPE dimension * sizeof(BF16)
    = 448 * 1 + 64 * 2
    = 576

scale groups
    = NoPE dimension / 64
    = 7

bytes per token
    = value bytes per token + reserved scale bytes
    = 576 + 8
    = 584
```

## 5. Page 内存布局

Raw page 不是简单的连续 `[token][584 bytes]` 记录，而是分区存储：

```text
+-----------------------------------------+
| 所有 token 的数据区：block_size * 576   |
+-----------------------------------------+
| 所有 token 的 scale 区：block_size * 8  |
+-----------------------------------------+
| page 尾部 alignment padding             |
+-----------------------------------------+
```

其中某个 token 的位置为：

```text
value_offset = page_base + token_offset * 576

scale_offset = page_base
             + block_size * 576
             + token_offset * 8
```

整个 page 向上对齐到 `576` 字节：

```text
page_bytes = round_up(block_size * 584, 576)
```

`576` 同时是一个 token 的有效数据区大小和 raw page 的对齐单位。

### 为什么 page 要向上对齐到 576 字节

`576` 不是通用的 GPU 硬件对齐值，它是当前 DeepSeek-V4 FlashMLA 一个 token
的有效 KV 数据行宽度。由于每个 page 的数据区已经由 `block_size` 个 576 字节的数据行组成，
对齐可以等价地写成：

```text
page_bytes
    = round_up(block_size * (576 + 8), 576)
    = block_size * 576 + round_up(block_size * 8, 576)
```

这表示 scale 区在 page 末尾占用整数个 576 字节数据行；不足一行的部分使用 padding 补齐。
因此所有 page 的基地址都满足：

```text
page_base = page_index * page_bytes
page_base % 576 = 0
```

底层 Sparse FlashMLA kernel 可以继续以 576 字节的数据行为单位解释每个 page，
不会让下一个 page 从上一个数据行的中间开始。

对 `block_size=64`，scale 区需要 `64 * 8 = 512` 字节，向上补成一个
576 字节数据行，因此产生 `576 - 512 = 64` 字节的 page 尾部 padding。

这个对齐属于当前 FlashMLA raw-cache ABI，而不是 C++ Tensor 的通用要求。
写入算子、cache view 和外部 Sparse FlashMLA kernel 必须使用一致的 `page_bytes`。
直接去掉 padding 可能使后续 page 被按错误基地址解释。

## 6. block_size=64 示例

```text
数据区大小 = 64 * 576 = 36864 bytes
scale 区   = 64 * 8   =   512 bytes
对齐前总计 =              37376 bytes
对齐后大小 =              37440 bytes
尾部 padding =               64 bytes
```

因此 `num_blocks=N`、`block_size=64` 时，Sparse FlashMLA raw cache 的形状是：

```text
[N, 37440], dtype=U8
```

## 7. U8 raw cache 与 F8 view

实际内存所有者是二维 U8 raw tensor：

```text
[num_blocks, page_bytes], dtype=U8
```

调用 Sparse FlashMLA 前，`DeepseekV4Attention` 会在同一块内存上创建 F8 strided view：

```text
[num_blocks, page_size, 1, 584], dtype=F8
```

这个 F8 tensor 是传递 raw-cache ABI 和地址信息的 view，不代表 page 内部真的按普通连续的 584 字节 token 结构排列。

### 为什么不直接定义为四维 KV cache

不能把真实的 cache 简单定义为普通连续的：

```text
[num_blocks, page_size, 1, 584]
```

普通连续四维 Tensor 隐含的逐 token 布局是：

```text
token 0: [576 bytes value][8 bytes scale]
token 1: [576 bytes value][8 bytes scale]
...
```

但当前 Sparse FlashMLA raw cache 的真实布局是：

```text
[value 0][value 1]...[value page_size-1]
[scale 0][scale 1]...[scale page_size-1]
[page padding]
```

因此两种区域的 token stride 不同：

```text
value 区的 token stride = 576 bytes
scale 区的 token stride = 8 bytes
```

某个 token 中字节的实际地址是分段计算的：

```text
address(block, token, byte):
    byte < 576:
        page_base + token * 576 + byte

    byte >= 576:
        page_base + page_size * 576
                  + token * 8
                  + (byte - 576)
```

普通 strided Tensor 只能用一组固定 stride 表达线性地址映射，无法让同一个 token
维度在 value 区使用 `576` stride，同时在 scale 区使用 `8` stride。
即使用 `strided_empty` 把 block stride 设置成对齐后的 `page_bytes`，也只能表达
page 之间的 padding，仍无法准确表达 value/scale 分区。

普通连续四维 Tensor 还会丢失 page 尾部 padding。例如 `page_size=64` 时：

```text
普通连续四维 Tensor 的 block stride = 64 * 584 = 37376 bytes
FlashMLA ABI 要求的 block stride       = 37440 bytes
```

直接按四维连续 Tensor 分配会让后续 block 的基地址提前 64 字节。

另外，这块 cache 的字节也不是语义上统一的 F8 数据，而是混合包含：

```text
448 字节 FP8 NoPE
128 字节 BF16 RoPE
  8 字节 UE8M0 scale 预留区
```

所以使用二维 U8 Tensor 作为内存所有者，可以更准确地表达“一块具有自定义格式的
raw byte storage”。`raw_cache_view_` 生成的四维 F8 view 只用于适配外部
Sparse FlashMLA 接口，不会复制、量化或重排数据，也不应交给通用 Tensor 算子解释。

当前结构的职责划分是：

```text
二维 U8 raw cache
    -> 负责内存所有权、完整 page 大小和自定义写入

四维 F8 strided view
    -> 负责适配外部 Sparse FlashMLA 的 Tensor 入参 ABI
```

后续可以考虑在创建 cache 时同时保存 `page_size` 和四维 view，避免
`raw_cache_view_` 每次根据 `page_bytes` 重新推断 `page_size`。但这只是减少重复包装，
底层仍然需要一个 raw byte owner，不能简单替换成普通连续四维 Tensor。

## 8. SGLang DeepSeek-V4 中 FlashMLA cache 的调用过程

本节基于 `/workspace/sglang` 分支 `v0.5.10_dpsk_v4_zkjh`、提交
`74e5f26d6` 的源码整理。

SGLang DeepSeek-V4 中没有直接调用一个名为 `flashmla_cache` 的公开
Python API。所谓“调用 FlashMLA cache”实际包含两个阶段：

1. 通过 `fused_store_flashmla_cache` 将新 KV 写成 FlashMLA 要求的 raw-cache 格式。
2. 通过 `flash_mla_with_kvcache` 按 `indices/topk_length` 读取 cache 并计算稀疏注意力。

完整调用链为：

```text
DeepseekV4Attention.forward
  |
  +-- 生成 q 和 kv
  |
  +-- DeepseekV4BackendRadix.store_cache
  |     |
  |     +-- set_swa_key_buffer_radix_fused
  |     +-- full slot -> SWA slot
  |     +-- fused_store_cache(type="flashmla")
  |     +-- FusedStoreCacheFlashMLAKernel::run
  |     +-- fused_store_flashmla_cache
  |
  +-- DeepseekV4BackendRadix.forward
        |
        +-- 取得并构造 k_cache 四维 view
        +-- 准备 indices/topk_length/sched_meta
        +-- flash_mla_with_kvcache_entrypoint
        +-- flash_mla.flash_mla_with_kvcache
        +-- flash_mla_cuda.sparse_decode_fwd
```

### 8.1 KV 的生成与写入

DeepSeek-V4 attention 先生成 `[num_tokens, 512]` 的 KV：

```text
448 维 NoPE + 64 维 RoPE = 512 维
```

默认配置启用 `SGLANG_OPT_USE_OVERLAP_STORE_CACHE`，模型可在独立 stream 上提前
调用 `DeepseekV4BackendRadix::store_cache`。未启用 overlap 时，backend 会在
attention `forward` 中写入，两条路径最终的 cache 格式相同。

默认的融合写入路径为：

```python
set_swa_key_buffer_radix_fused(
    layer_id=layer_id,
    raw_loc=forward_batch.out_cache_loc,
    cache_k=swa_k,
)
```

`raw_loc` 先通过 `full_to_swa_index_mapping` 从全局 KV slot 转换成 SWA cache
slot，然后进入：

```python
fused_store_cache(
    input=cache_k,
    cache=kv_buffer,
    indices=swa_loc,
    page_size=page_size,
    type="flashmla",
)
```

`type="flashmla"` 会选择 `FusedStoreCacheFlashMLAKernel`，最终启动
`fused_store_flashmla_cache` GPU kernel。该 kernel 完成 NoPE FP8 量化、UE8M0
scale 生成、RoPE BF16 转换和按 slot 写入。

### 8.2 cache 的分配与 FlashMLA view

SGLang 使用 `DeepSeekV4SingleKVPool` 为每层分配二维 raw byte storage：

```text
[num_pages, round_up(page_size * 584, 576)], dtype=uint8
```

SWA、C4 和 C128 分别使用独立 pool：

```text
SWA  page_size = 256
C4   page_size = 256 / 4   = 64
C128 page_size = 256 / 128 = 2
```

调用 FlashMLA 前，backend 取得这个二维 cache，裁掉每页末尾的
alignment padding，再在同一块内存上创建四维 view：

```text
[num_pages, page_size, 1, 584]
```

SWA 路径的对应代码语义为：

```python
swa_k_cache = swa_k_cache[:, :swa_window_size * 584].view(
    num_pages, swa_window_size, 1, 584
)
```

二维 raw cache 的第一维 stride 仍是对齐后的 `page_bytes`，因此创建 view
不会丢失 page padding。最后一维 `584` 只是 FlashMLA V4 用来描述逻辑
cache 行宽的 ABI 参数，不代表物理上真的是逐 token 连续的 584 字节
结构。其物理布局仍然是本文第 5 节记录的 value/scale 分区布局。

### 8.3 `raw_out_loc`、`swa_loc` 和 `swa_page_indices`

三者都和 cache slot 有关，但用途不同：

| 变量 | 含义 | 用途 |
| --- | --- | --- |
| `raw_out_loc` / `out_cache_loc` | 本轮新 KV 的全局 slot | 确定新 KV 写到哪里 |
| `swa_loc` | `raw_out_loc` 映射后的 SWA slot | 传给 fused store kernel |
| `swa_page_indices` | 每个 query 需要读取的 SWA token slot 列表 | 传给 FlashMLA sparse attention |

`swa_page_indices` 对每个 query 选取最近 128 个 token：

```python
offsets = current_position - arange(128)
raw_indices = req_to_token[request, offsets]
swa_page_indices = full_to_swa_index_mapping[raw_indices]
```

序列左侧越界的位置填 `-1`；每个 query 的实际有效读取长度为：

```python
swa_topk_lengths = clamp(seq_len, max=128)
```

FlashMLA 只处理 `swa_page_indices` 最左侧 `swa_topk_lengths` 个项。索引
最后一维还会补齐到 64 的倍数，补齐项为 `-1`。

### 8.4 FlashMLA 入参和 sparse 路径

SGLang 构造的核心参数为：

```python
flash_mla_with_kvcache(
    q=q,
    k_cache=swa_k_cache,
    head_dim_v=512,
    block_table=None,
    cache_seqlens=None,
    tile_scheduler_metadata=flashmla_metadata,
    softmax_scale=512**-0.5,
    is_fp8_kvcache=True,
    indices=swa_page_indices,
    topk_length=swa_topk_lengths,
    attn_sink=attn_sink,
    extra_k_cache=extra_k_cache,
    extra_indices_in_kvcache=extra_indices,
    extra_topk_length=extra_topk_lengths,
)
```

`indices` 非空代表使用 FlashMLA sparse 模式。此时：

- `block_table` 和 `cache_seqlens` 明确传 `None`。
- `indices` 里的数值是 cache 中的绝对 token slot，其语义为
  `page_index * page_size + offset_in_page`。
- `topk_length` 表示每个 query 真正有效的索引数量。
- `causal` 在 sparse 模式下为 `false`，因果关系已由 indices 显式表达。

`flash_mla_with_kvcache_entrypoint` 根据 `SGLANG_HACK_FLASHMLA_BACKEND` 选择后端。
默认值为 `kernel`，调用外部 `flash_mla.flash_mla_with_kvcache`，最终进入
`flash_mla_cuda.sparse_decode_fwd`。`torch_native` 只是用于调试和参考的
Python 实现，不是生产路径。

### 8.5 C1/C4/C128 与 `extra_k_cache`

所有层都会使用 SWA cache 注意最近 128 个 token。对压缩层，还会在
同一次 FlashMLA 调用中增加历史 cache：

| `compress_ratio` | 参与计算的 cache |
| --- | --- |
| `0` | SWA cache |
| `4` | SWA cache + C4 `extra_k_cache`，C4 最多选择 512 个 token |
| `128` | SWA cache + C128 `extra_k_cache` |

`extra_k_cache` 使用和 SWA cache 相同的 584-byte raw-cache ABI，同时提供
`extra_indices_in_kvcache` 和 `extra_topk_length`。SWA 和压缩 cache 不是分两次
attention 计算，而是在一次 `sparse_decode_fwd` 内合并，共享同一次
softmax 归一化。

### 8.6 FlashMLA scheduler metadata 的生命周期

SGLang 为 C1、C4 和 C128 分别保存一个 `FlashMLASchedMeta`：

```text
c1_flashmla_metadata
c4_flashmla_metadata
c128_flashmla_metadata
```

`flash_mla.get_mla_metadata()[0]` 当前只创建一个未初始化的 metadata 对象。
第一次调用 `flash_mla_with_kvcache` 时，FlashMLA 根据以下配置创建
tile scheduler 数据：

```text
batch size
query sequence length
query/KV head count
cache page size
is_fp8_kvcache
indices 的 topk 维度
extra cache page size
extra indices 的 topk 维度
```

调用结束后，FlashMLA 把新的 `tile_scheduler_metadata` 和 `num_splits` 写回
`FlashMLASchedMeta`，后续调用直接复用。复用的前提是 tensor shape 以及
`cache_seqlens/topk_length/extra_topk_length` 保持一致。CUDA graph capture/replay
中也必须保持 metadata tensor 的所有权和地址稳定。

### 8.7 Decode 和 prefill 的 shape 处理

Decode 时 backend 将单 token 输入整理为 FlashMLA 需要的：

```text
q       : [batch, 1, num_heads, 512]
indices : [batch, 1, padded_topk]
```

Prefill 如果 batch 内各序列的 extend length 相同，会将 token-major 输入整理为：

```text
q       : [batch, seq_len_q, num_heads, 512]
indices : [batch, seq_len_q, padded_topk]
```

如果不满足该 reshape 条件，当前 backend 会复用 decode 形式的调用路径。
这里虽然 Python 函数名为 `sparse_decode_fwd`，但它接受 `seq_len_q > 1`
的输入布局；实际支持范围仍以当前外部 FlashMLA kernel 为准。

### 8.8 对 InfiniLM 实现的启示

SGLang 的职责划分可以概括为：

```text
out_cache_loc/raw_out_loc
    -> 写入位置

full_to_swa_index_mapping
    -> 全局 slot 与 SWA slot 之间的转换

swa_page_indices + swa_topk_lengths
    -> 稀疏读取集合

U8 raw cache
    -> 内存所有权、value/scale 分区布局和 page padding

4D FlashMLA view
    -> 外部 kernel 的 Tensor 入参 ABI

FlashMLASchedMeta
    -> 可复用的 kernel 调度数据
```

因此，`raw_out_loc` 不能仅因为它和 `block_table` 都涉及 cache slot 就直接
互换：前者表示本轮写入位置，而 dense attention 中的 `block_table` 表示整个
序列的逻辑页到物理页映射。SGLang DeepSeek-V4 sparse FlashMLA 路径甚至不传
`block_table`，而是直接传入绝对 token slot 列表 `indices`。

## 9. 修改注意事项

这四个量的关系属于 DeepSeek-V4 Sparse FlashMLA cache ABI。修改模型维度、量化分组或 cache 格式时，必须同步检查：

```text
InfiniLM:
  csrc/layers/mla_attention/flashmla.cpp
  csrc/models/deepseek_v4/deepseek_v4_attention.cpp
  csrc/models/deepseek_v4/deepseek_v4_for_causal_lm.cpp

InfiniCore:
  src/infinicore/ops/deepseek_v4/fused_store_flashmla_cache/fused_store_flashmla_cache.cc

底层 FlashMLA:
  sparse cache 读取 kernel 对 shape、stride、scale 和 page alignment 的约定
```

不能只修改 allocator 中的维度计算，否则写入算子和 FlashMLA 读取 kernel 会按照不同布局解释同一块内存。
